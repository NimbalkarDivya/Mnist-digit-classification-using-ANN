# Mnist-digit-classification-using-ANN
This project implements a handwritten digit classification system using an Artificial Neural Network (ANN) trained on the MNIST dataset. The model learns to recognize digits (0–9) from grayscale images and predicts the correct digit based on pixel patterns.
